# 🚀 START HERE - Rossmann Sales Recommendation System

## 👋 Добро пожаловать!

Это полнофункциональная система прогнозирования продаж и формирования управленческих рекомендаций для сети магазинов Rossmann, реализованная согласно вашей курсовой работе (chapter3.tex).

## ✅ Что уже сделано

**Все компоненты из chapter3.tex успешно реализованы:**

✅ DataLoader - загрузка данных из PostgreSQL  
✅ FeatureEngineer - генерация признаков  
✅ SalesForecaster - прогноз с XGBoost (с мок-моделью)  
✅ SimilarityService - поиск похожих наблюдений (KNN)  
✅ RuleEngine - 4 правила рекомендаций  
✅ RecommendationService - оркестратор  
✅ FastAPI REST API с endpoint /predict  
✅ PostgreSQL интеграция  
✅ Полная документация  

## 🎯 Что нужно сделать ВАМ

### Вариант 1: Быстрый старт (с мок-моделью)

```bash
cd project
pip install -r requirements.txt
createdb rossmann_db
echo "DATABASE_URL=postgresql://user:pass@localhost/rossmann_db" > .env
python init_db.py
python run_server.py
```

**Время: ~10 минут**

### Вариант 2: Полная установка (с реальной моделью)

Следуйте детальному чеклисту: **`INSTALLATION_CHECKLIST.md`**

**Время: ~30 минут**

## 📚 Навигация по документации

| Файл | Когда читать | Время |
|------|--------------|-------|
| **START_HERE.md** | ▶️ Прямо сейчас | 2 мин |
| **QUICKSTART.md** | Для быстрого запуска | 5 мин |
| **INSTALLATION_CHECKLIST.md** | Для пошаговой установки | 10 мин |
| **README.md** | Для полного понимания | 15 мин |
| **ARCHITECTURE.md** | Для изучения архитектуры | 10 мин |
| **EXAMPLES.md** | Для примеров использования | 10 мин |
| **SUMMARY.md** | Для отчёта о реализации | 5 мин |
| **PROJECT_OVERVIEW.md** | Для общего обзора | 5 мин |

## 🎓 Для защиты курсовой

### Быстрая подготовка (15 минут)

1. **Запустите систему** (следуйте QUICKSTART.md)
2. **Откройте Swagger UI** - http://localhost:8000/docs
3. **Запустите test_api.py** - покажите работающие примеры
4. **Изучите SUMMARY.md** - там таблица соответствия chapter3.tex

### Что показать на защите

1. **Схему архитектуры** из PROJECT_OVERVIEW.md
2. **Работающий API** через Swagger UI
3. **Примеры запросов** из EXAMPLES.md
4. **Соответствие коду** из chapter3.tex (см. SUMMARY.md)

## 🔧 Как заменить мок-модель на реальную

После обучения модели в `train_models.py`:

```python
import pickle

# Сохранить модель
with open('project/models/xgboost_model.pkl', 'wb') as f:
    pickle.dump(xgb_model, f)

# Также нужно сохранить scaler
with open('project/models/scaler.pkl', 'wb') as f:
    pickle.dump(scaler, f)
```

Затем перезапустите сервер - он автоматически загрузит обученную модель.

## 📊 Структура проекта (упрощенная)

```
project/
├── 📁 app/                    # Основное приложение
│   ├── main.py               # ⭐ FastAPI (endpoint /predict)
│   ├── database.py           # PostgreSQL
│   └── 📁 models/            # Бизнес-логика
│       ├── data_loader.py           ⭐
│       ├── feature_engineer.py      ⭐
│       ├── sales_forecaster.py      ⭐
│       ├── similarity_service.py    ⭐
│       ├── rule_engine.py           ⭐
│       └── recommendation_service.py ⭐
│
├── 📁 models/                 # ML модели
│   └── xgboost_model.pkl     # ← Поместите сюда вашу модель
│
├── 🔧 init_db.py             # Загрузка данных в PostgreSQL
├── 🚀 run_server.py          # Запуск сервера
├── 🧪 test_api.py            # Тестирование
│
└── 📚 Документация (8 файлов)
    ├── START_HERE.md         # ← Вы здесь
    ├── QUICKSTART.md
    ├── README.md
    └── ...
```

⭐ = Соответствует разделу 3.2 из chapter3.tex

## 🎬 Первые шаги

### Шаг 1: Проверьте окружение

```bash
python --version  # должно быть 3.9+
psql --version    # должен быть установлен PostgreSQL
```

### Шаг 2: Установите зависимости

```bash
cd project
pip install -r requirements.txt
```

### Шаг 3: Настройте базу данных

```bash
# Создайте БД
createdb rossmann_db

# Создайте .env файл
echo "DATABASE_URL=postgresql://postgres:password@localhost:5432/rossmann_db" > .env
```

⚠️ Замените `postgres:password` на ваши credentials!

### Шаг 4: Загрузите данные

```bash
python init_db.py
```

Ожидайте ~2-5 минут.

### Шаг 5: Запустите сервер

```bash
python run_server.py
```

Ожидайте ~30-60 секунд для инициализации.

### Шаг 6: Проверьте работу

Откройте в браузере:
- http://localhost:8000/docs - Swagger UI
- http://localhost:8000/health - проверка здоровья

Или запустите тесты:

```bash
# В новом терминале
python test_api.py
```

## ✨ Пример использования

```bash
curl -X POST "http://localhost:8000/predict" \
  -H "Content-Type: application/json" \
  -d '{
    "store_id": 1,
    "date": "2015-08-01",
    "promo": 1
  }'
```

**Ответ:**
```json
{
  "forecast_sales": 7542.30,
  "benchmark_sales": 8125.50,
  "recommendations": [
    "Revenue is below typical (7542 vs 8125). Explore practices of the best stores.",
    "75% of successful cases included a promotion. Consider the promotion."
  ]
}
```

## 🆘 Нужна помощь?

### Быстрые решения

**Ошибка подключения к БД:**
→ Проверьте PostgreSQL запущен и .env правильный

**Module not found:**
→ `pip install -r requirements.txt`

**Port 8000 занят:**
→ Измените порт в run_server.py

**Медленная инициализация:**
→ Это нормально! Система загружает ~800K записей

### Детальные решения

См. секцию Troubleshooting в:
- `INSTALLATION_CHECKLIST.md`
- `README.md`

## 📞 Полезные ссылки

После запуска сервера:

- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc
- **Health Check**: http://localhost:8000/health
- **Root**: http://localhost:8000

## 🎯 Следующие шаги

1. ✅ Запустите систему (QUICKSTART.md)
2. 📖 Изучите архитектуру (ARCHITECTURE.md)
3. 💡 Попробуйте примеры (EXAMPLES.md)
4. 🤖 Замените мок-модель на реальную
5. 🎓 Подготовьтесь к защите (SUMMARY.md)

## 🎉 Готово!

Система полностью реализована и готова к использованию.

Все модули из chapter3.tex работают, API функционирует, документация полная.

**Удачи с защитой курсовой работы! 🎓**

---

## 📋 Quick Reference

| Команда | Действие |
|---------|----------|
| `python run_server.py` | Запустить сервер |
| `python test_api.py` | Запустить тесты |
| `python init_db.py` | Загрузить данные в БД |
| `curl http://localhost:8000/health` | Проверить здоровье |

| URL | Назначение |
|-----|-----------|
| http://localhost:8000 | Главная страница API |
| http://localhost:8000/docs | Swagger UI |
| http://localhost:8000/health | Health check |
| http://localhost:8000/predict | Прогноз (POST) |

---

**Начните с QUICKSTART.md для быстрого запуска! ⚡**

