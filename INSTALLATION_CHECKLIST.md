# 📋 Installation & Setup Checklist

Пошаговый чеклист для установки и запуска системы.

## ✅ Pre-installation

- [ ] Python 3.9+ установлен (`python --version`)
- [ ] PostgreSQL установлен и запущен (`psql --version`)
- [ ] pip обновлен (`pip install --upgrade pip`)
- [ ] Есть доступ к файлам `train.csv` и `store.csv` в родительской папке

## ✅ Step 1: Установка зависимостей

```bash
cd project
pip install -r requirements.txt
```

**Проверка:**
- [ ] Все пакеты установлены без ошибок
- [ ] `import fastapi` работает в Python

## ✅ Step 2: Настройка PostgreSQL

### 2.1 Создание базы данных

```bash
# Вариант 1: через команду
createdb rossmann_db

# Вариант 2: через psql
psql -U postgres
CREATE DATABASE rossmann_db;
\q
```

**Проверка:**
- [ ] База данных создана
- [ ] Можно подключиться: `psql -d rossmann_db`

### 2.2 Создание .env файла

```bash
# Windows PowerShell
echo "DATABASE_URL=postgresql://postgres:password@localhost:5432/rossmann_db" > .env

# Linux/Mac
echo "DATABASE_URL=postgresql://postgres:password@localhost:5432/rossmann_db" > .env
```

**Важно:** Замените `postgres` и `password` на ваши учетные данные!

**Проверка:**
- [ ] Файл `.env` создан
- [ ] Содержит правильный DATABASE_URL

## ✅ Step 3: Загрузка данных в БД

```bash
cd project
python init_db.py
```

**Ожидаемый вывод:**
```
Creating database tables...
Tables created successfully!
Loading CSV files...
Loaded 1017209 sales records and 1115 stores
Inserting store data...
Inserted 1115 stores
Inserting sales data...
Inserted 10000/1017209 sales records
...
✅ Database initialization completed successfully!
```

**Время выполнения:** ~2-5 минут

**Проверка:**
- [ ] Скрипт завершился без ошибок
- [ ] Выведено "Database initialization completed successfully!"
- [ ] В БД есть данные:
  ```sql
  psql -d rossmann_db
  SELECT COUNT(*) FROM sales;   -- должно быть ~800K после фильтрации
  SELECT COUNT(*) FROM stores;  -- должно быть 1115
  \q
  ```

## ✅ Step 4: (Опционально) Обучение модели

Если хотите использовать реальную модель вместо мок-модели:

```bash
cd ..  # возврат в корневую папку
python train_models.py
```

После обучения сохраните модель:

```python
# Добавьте в конец train_models.py
import pickle
with open('project/models/xgboost_model.pkl', 'wb') as f:
    pickle.dump(xgb_model, f)
print("✅ Model saved to project/models/xgboost_model.pkl")
```

**Проверка:**
- [ ] Файл `project/models/xgboost_model.pkl` создан
- [ ] Размер файла > 0 KB

## ✅ Step 5: Запуск сервера

```bash
cd project
python run_server.py
```

**Ожидаемый вывод:**
```
============================================================
Starting Rossmann Sales Recommendation System
============================================================
...
============================================================
Initializing RecommendationService...
============================================================
Loaded 817288 valid observations
Feature matrix shape: (817288, 30)
✅ SimilarityService fitted on 817288 observations
✅ RecommendationService initialized successfully
============================================================

✅ System ready to accept requests!
============================================================

INFO:     Uvicorn running on http://0.0.0.0:8000
```

**Время инициализации:** ~30-60 секунд

**Проверка:**
- [ ] Сервер запустился без ошибок
- [ ] Выведено "System ready to accept requests!"
- [ ] Нет красных ERROR сообщений

## ✅ Step 6: Тестирование API

### 6.1 Через браузер

Откройте в браузере:
- [ ] http://localhost:8000 - основная страница
- [ ] http://localhost:8000/docs - Swagger UI
- [ ] http://localhost:8000/health - health check

### 6.2 Через test_api.py

Откройте **новый терминал** (сервер должен продолжать работать):

```bash
cd project
python test_api.py
```

**Ожидаемый вывод:**
```
============================================================
API Testing Script
============================================================
...
Testing POST /predict
...
✅ Prediction successful!
Forecast Sales: 7542.30
...
============================================================
✅ All tests completed!
============================================================
```

**Проверка:**
- [ ] Все тесты прошли успешно
- [ ] Получены прогнозы и рекомендации
- [ ] Нет ошибок подключения

### 6.3 Через curl (опционально)

```bash
curl -X POST "http://localhost:8000/predict" \
  -H "Content-Type: application/json" \
  -d '{"store_id": 1, "date": "2015-08-01", "promo": 1}'
```

**Проверка:**
- [ ] Получен JSON ответ
- [ ] Содержит forecast_sales, benchmark_sales, recommendations

## ✅ Troubleshooting

### Проблема: "Could not connect to database"

**Решение:**
1. Проверьте, что PostgreSQL запущен
2. Проверьте credentials в `.env`
3. Проверьте, что база данных создана

```bash
# Проверка статуса PostgreSQL (Windows)
pg_ctl status

# Проверка статуса (Linux/Mac)
sudo systemctl status postgresql

# Проверка баз данных
psql -U postgres -l
```

### Проблема: "Module not found"

**Решение:**
```bash
pip install -r requirements.txt --force-reinstall
```

### Проблема: "Port 8000 already in use"

**Решение:**
```bash
# Изменить порт в run_server.py
uvicorn.run("app.main:app", port=8001)  # вместо 8000
```

### Проблема: Медленная инициализация

**Это нормально!** Система загружает ~800K записей в память для KNN-поиска.

Ожидаемое время:
- 30-60 секунд для полного датасета
- Выполняется только при старте сервера
- Запросы после инициализации быстрые (50-100ms)

### Проблема: "Mock model warning"

**Это нормально!** Если вы не обучили модель, используется мок-модель.

Чтобы использовать реальную модель:
1. Обучите модель (`train_models.py`)
2. Сохраните в `project/models/xgboost_model.pkl`
3. Перезапустите сервер

## ✅ Final Checks

После успешной установки:

- [ ] Сервер запускается без ошибок
- [ ] API отвечает на запросы
- [ ] Тесты проходят успешно
- [ ] Swagger UI доступен
- [ ] Прогнозы возвращаются в разумных пределах (1000-20000)
- [ ] Рекомендации генерируются

## 🎉 Success!

Если все пункты отмечены ✅, система готова к использованию!

### Что дальше?

1. **Изучите примеры** - см. `EXAMPLES.md`
2. **Прочитайте архитектуру** - см. `ARCHITECTURE.md`
3. **Обучите реальную модель** - замените мок-модель
4. **Интегрируйте в свои системы** - используйте REST API

## 📞 Полезные команды

```bash
# Запуск сервера
python run_server.py

# Тестирование
python test_api.py

# Проверка БД
psql -d rossmann_db -c "SELECT COUNT(*) FROM sales;"

# Остановка сервера
# Ctrl+C в терминале сервера

# Проверка health
curl http://localhost:8000/health
```

---

**Удачи с запуском системы! 🚀**

